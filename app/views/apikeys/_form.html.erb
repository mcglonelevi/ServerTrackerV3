<%= form_for(apikey) do |f| %>
  <% if apikey.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(apikey.errors.count, "error") %> prohibited this apikey from being saved:</h2>

      <ul>
      <% apikey.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field form-group">
    <label for="">Public Key</label>
    <%= f.text_field :publickey, class: "form-control" %>
  </div>

  <div class="field form-group">
    <label for="">Private Key</label>
    <%= f.text_field :privatekey, class: "form-control" %>
  </div>

  <div class="actions">
    <%= f.submit "Update/Create API Key", class: "btn btn-success" %>
  </div>
<% end %>
