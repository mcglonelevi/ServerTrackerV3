<%= form_for([@error_event.server, @error_event]) do |f| %>
  <% if @error_event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@error_event.errors.count, "error") %> prohibited this error_event from being saved:</h2>

      <ul>
      <% @error_event.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field form-group">
    <%= f.label :message %><br />
    <%= f.text_field :message, class: "form-control" %>
  </div>
  <div class="actions">
    <%= f.submit "Create Error Event", class: "btn btn-success" %>
  </div>
<% end %>
